# Estado de entrega de certificados

Este directorio almacena temporalmente el estado de entrega de certificados en `certificados_entrega.json`.

No modifica la base de datos de producción.

El archivo debe ser escribible por PHP y no debe ser accesible públicamente. El `.htaccess` incluido bloquea el acceso directo en Apache.
